import whenBlocks from './blocks/when';
import andBlocks from './blocks/and';
import thenBlocks from './blocks/then';
import specialBlocks from './blocks/special';

const blocks = {
    when: whenBlocks,
    and: andBlocks,
    then: thenBlocks,
    special: specialBlocks
}

export default blocks;